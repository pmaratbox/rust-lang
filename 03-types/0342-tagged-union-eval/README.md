# 0342 — Tagged Union Eval

Model an expression tagged union Num | Add and evaluate Add(Num 1, Num 2), printing `3`. Rust enums are true sum types matched exhaustively with `match`.

## Run

    cargo run
