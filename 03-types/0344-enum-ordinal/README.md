# 0344 — Enum Ordinal

For the enum Direction(N,E,S,W) print the ordinal of S (2) and the name at ordinal 3 (W), printing `2 W`. Rust fieldless enums cast to their discriminant with `as usize` and derive `Debug` for names.

## Run

    cargo run
