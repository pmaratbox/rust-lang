# 0345 — Struct Defaults

Create a Point with default fields (0,0), then one overriding x to 5, printing each on its own line `0 0` then `5 0`. Rust derives `Default` and uses struct update syntax `..Default::default()` to override one field.

## Run

    cargo run
