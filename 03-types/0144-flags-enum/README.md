# 0144 — Flag Enum (Bit Flags)

Combine bit flags READ(1) and WRITE(2) into 3, check that WRITE is set, and print `3 yes`. Rust models bit flags with `const` integers and bitwise operators on unsigned types.

## Run

    cargo run
