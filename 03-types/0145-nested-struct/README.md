# 0145 — Nested Struct

Model a person whose address is a nested struct, and print the city `London`. Rust composes plain `struct` types by value, so `person.address.city` reaches the nested field directly.

## Run

    cargo run
