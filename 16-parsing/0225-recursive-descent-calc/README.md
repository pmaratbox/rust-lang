# 0225 — Recursive Descent Calculator

Parse and evaluate "2+3*4" honoring precedence, printing `14`. Rust models the cursor with a `Parser` struct and mutually recursive methods.

## Run

    cargo run
