# 0230 — Parse JSON Array

Parse the JSON array text "[1,2,3]" and print the sum of its elements `6`. Rust strips the brackets, splits on commas, and folds with `Iterator::sum`.

## Run

    cargo run
