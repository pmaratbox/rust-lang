# 0232 — Word/Line/Char Counts

For the text "a b\nc" report words, lines, and characters, printing `3 2 5`. Rust uses `split_whitespace`, `lines`, and `chars` iterators counted with `count`.

## Run

    cargo run
