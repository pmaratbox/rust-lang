# 0231 — Template Substitution

Substitute the variable in the template "hi {name}" with name="Ada", printing `hi Ada`. Rust scans for `{key}` spans and looks each up in a `HashMap`.

## Run

    cargo run
