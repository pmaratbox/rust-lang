# 0269 — Deterministic Transition Walk

Walk a deterministic transition map A->B->C->A for three steps from A, printing the visited states `B C A`. A `HashMap` built from an array of pairs holds the transition function.

## Run

    cargo run
