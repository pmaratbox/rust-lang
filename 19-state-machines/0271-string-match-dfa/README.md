# 0271 — DFA String Match

Build a DFA for the pattern "ab" and report the first match index in "aab", printing `1`. The DFA state counts how many pattern bytes have matched so far.

## Run

    cargo run
