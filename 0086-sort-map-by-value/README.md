# 0086 — Sort a Map by Value

Sort the map `{a: 3, b: 1, c: 2}` by value in ascending order and print the entries: `b:1 c:2 a:3`. The map's entries are collected into a `Vec` and `sort_by_key` orders them by the value field.

## Run

    cargo run
