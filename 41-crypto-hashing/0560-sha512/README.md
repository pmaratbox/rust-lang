# 0560 — SHA-512

Compute the SHA-512 digest of the fixed string `hello` using the `sha2`
crate's `Sha512` hasher (via the `Digest` trait). The 64-byte digest is
formatted as a lowercase hexadecimal string and printed.

## Run

    cargo run
