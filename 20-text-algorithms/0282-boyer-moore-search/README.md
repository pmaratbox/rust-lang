# 0282 — Boyer-Moore Search

Use the bad-character rule to find "abc" in "zzabc", printing the index `2`. A `[usize; 256]` last-occurrence table implements the shift idiomatically.

## Run

    cargo run
