# 0283 — Rabin-Karp Search

Use rolling-hash Rabin-Karp to find all start indices of "ab" in "xabxab", printing `1 4`. `wrapping_*` arithmetic on `u64` keeps the rolling hash safe.

## Run

    cargo run
