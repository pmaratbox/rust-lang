# 0278 — Suffix Array

Build the suffix array of "banana" (sorted suffix start indices), printing `5 3 1 0 4 2`. `sort_by_key` over string slices expresses the suffix ordering directly.

## Run

    cargo run
