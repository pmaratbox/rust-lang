# 0256 — Hamming Distance

Compute the bit Hamming distance between 1 and 4, printing `2`. Rust's `count_ones` is the idiomatic population count over the XOR of the two values.

## Run

    cargo run
