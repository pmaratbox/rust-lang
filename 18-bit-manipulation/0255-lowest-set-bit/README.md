# 0255 — Lowest Set Bit

Isolate the lowest set bit of 12 (1100), printing `4`. On unsigned types Rust uses `wrapping_neg` to compute the two's-complement negation for the `x & (-x)` idiom.

## Run

    cargo run
