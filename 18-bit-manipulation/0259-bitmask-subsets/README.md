# 0259 — Enumerate Submasks

Enumerate all submasks of the mask 5 (101) in descending order `5 4 1 0`. A `loop` with `(sub - 1) & mask` walks every submask down to zero before breaking.

## Run

    cargo run
