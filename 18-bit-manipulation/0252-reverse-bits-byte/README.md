# 0252 — Reverse Bits in a Byte

Reverse the 8 bits of the value 1 (00000001) to get 128 (10000000), printing `128`. Rust's `u8::reverse_bits` reverses the bit order within the byte's width.

## Run

    cargo run
