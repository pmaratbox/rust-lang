# 0261 — Rotate Left (8-bit)

Rotate the 8-bit value 1 left by 1 (->2) and 128 left by 1 (->1), printing `2 1`. Rust's `u8::rotate_left` performs a true width-aware rotation, wrapping the top bit around.

## Run

    cargo run
