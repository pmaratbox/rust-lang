# 0253 — Power of Two Check

Test whether 16 (yes) and 18 (no) are powers of two, printing `yes no`. The `n & (n - 1)` trick clears the lowest set bit, which is zero only for powers of two.

## Run

    cargo run
