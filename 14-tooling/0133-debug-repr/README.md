# 0133 — Debug Representation

Produce a debug/structured representation of a Point(1,2) and print `Point(x=1, y=2)`. A custom `impl fmt::Debug` formats the struct for the `{:?}` placeholder.

## Run

    cargo run
