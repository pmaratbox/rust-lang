# 0132 — Mini Test Runner

Run three named test functions that all pass and report `3 passed, 0 failed`. A `Vec` of `fn() -> bool` function pointers is iterated to tally results.

## Run

    cargo run
