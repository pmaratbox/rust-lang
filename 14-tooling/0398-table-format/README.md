# 0398 — Aligned Table

Format the rows (a,1),(bb,22) with the first column left-padded to the widest value, printing two aligned rows. Rust's `{:<width$}` format spec left-aligns each first cell to the computed width.

## Run

    cargo run
