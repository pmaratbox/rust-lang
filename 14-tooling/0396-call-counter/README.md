# 0396 — Call Counter

Wrap a function so each call increments a counter; after five calls print `calls: 5`. A closure capturing a `Cell<u32>` counts invocations without mutable aliasing.

## Run

    cargo run
