# 0689 — Take

Uses Rust's `itertools` crate alongside the standard iterator `take` adapter to take the first 3 elements of `[1..=10]`, then comma-joins them with itertools' `join`, yielding `1,2,3`.

## Run

    cargo run
