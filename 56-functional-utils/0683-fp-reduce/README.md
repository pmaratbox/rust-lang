# 0683 — Reduce

Uses Rust's `itertools` crate alongside the standard iterator's `fold` reducer to collapse `[1, 2, 3, 4, 5]` to a single value by repeatedly applying addition starting from an initial accumulator of `0`, yielding `15`.

## Run

    cargo run
