# 0381 — Parallel Map

Square 1,2,3,4 in parallel and collect the results in input order, printing `1 4 9 16`. Spawning threads in index order and joining them in the same order preserves the input ordering.

## Run

    cargo run
