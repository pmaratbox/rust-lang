# 0096 — Mutual Recursion

Using two mutually recursive functions `isEven` and `isOdd` (each calling the other), report whether `4` and `3` are even, printing `even` and `odd`. Items in a module are visible regardless of order, so the two functions call each other freely down to the zero base case.

## Run

    cargo run
