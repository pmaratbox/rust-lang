# 0142 — Countdown by Recursion

Recursively count down from 5 to 1, printing `5 4 3 2 1` on one line. Each recursive call pushes its value into a shared `Vec`, joined with spaces at the end.

## Run

    cargo run
