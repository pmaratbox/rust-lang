# 0338 — Return Min and Max

Return both the minimum and maximum of [4,1,7] from one function and print `1 7`. Rust returns multiple values as a tuple, destructured at the call site.

## Run

    cargo run
