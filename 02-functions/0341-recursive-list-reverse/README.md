# 0341 — Recursive List Reverse

Reverse [1,2,3] using recursion (no built-in reverse), printing `3 2 1`. Rust slice patterns recurse on the tail, then push the head onto the result.

## Run

    cargo run
