# 0141 — Ackermann Function

Compute the Ackermann function A(2,3) recursively and print `9`. Rust's `else if` chain expresses the three Ackermann cases without explicit returns.

## Run

    cargo run
