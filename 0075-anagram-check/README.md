# 0075 — Anagram Check

Check whether each pair is an anagram by comparing sorted letters — `listen`/`silent` and `hello`/`world` — printing `listen/silent: yes` and `hello/world: no`. `sorted` collects and sorts the `char`s; comparing the two sorted `Vec<char>`s decides the anagram.

## Run

    cargo run
