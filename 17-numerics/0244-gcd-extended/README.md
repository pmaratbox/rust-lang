# 0244 — Extended Euclid

Compute extended gcd(30,12) returning g and Bezout coefficients, printing `6 1 -2` (since 30*1+12*(-2)=6). Recursion returns a `(g, x, y)` tuple destructured at each step.

## Run

    cargo run
