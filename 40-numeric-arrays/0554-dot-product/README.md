# 0554 — Dot product

Computes the dot product of two integer vectors `[1, 2, 3]` and `[4, 5, 6]` using the `ndarray` crate's `Array1::dot` method, which multiplies the vectors element-wise and sums the products. The result is printed as a single integer (`32`).

## Run

    cargo run
