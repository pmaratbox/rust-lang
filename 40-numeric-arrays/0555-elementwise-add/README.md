# 0555 — Element-wise add

Uses the `ndarray` crate's element-wise addition (`&a + &b`) on two
`Array1<i32>` vectors. The library adds corresponding elements of
`[1, 2, 3]` and `[10, 20, 30]`, and the result is printed as
space-joined integers.

## Run

    cargo run
