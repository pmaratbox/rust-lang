# 0558 — Scalar multiply

Uses the `ndarray` crate's elementwise scalar-multiplication operator (`&a * scalar`)
to multiply the matrix `[[1, 2], [3, 4]]` by the integer scalar `3`. Each result row
is printed as space-joined integers.

## Run

    cargo run
