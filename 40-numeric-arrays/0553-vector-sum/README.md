# 0553 — Vector sum

Build the 1-D array `[1, 2, 3, 4]` with the `ndarray` crate's `array!`
macro and reduce it to a single integer total using `ndarray`'s `.sum()`
operation. The result is printed as an integer.

## Run

    cargo run
