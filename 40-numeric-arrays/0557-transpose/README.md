# 0557 — Transpose

Transposes the integer matrix `[[1, 2, 3], [4, 5, 6]]` using the `ndarray` crate's `t()` method, which returns a transposed view that swaps the rows and columns into a 3x2 matrix. Each resulting row is printed as space-joined integers.

## Run

    cargo run
