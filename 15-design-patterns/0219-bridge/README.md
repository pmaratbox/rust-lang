# 0219 — Bridge

Bridge a Circle abstraction to a Red color implementation, printing `red circle`. The `Circle` holds a `Box<dyn Color>` implementor decoupling shape from color.

## Run

    cargo run
