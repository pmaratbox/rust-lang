# 0202 — Abstract Factory

Use a "dark" theme abstract factory to build a button and a checkbox, printing `dark-button dark-checkbox`. A `ThemeFactory` trait yields boxed `Button` and `Checkbox` trait objects.

## Run

    cargo run
