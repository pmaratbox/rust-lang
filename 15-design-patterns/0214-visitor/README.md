# 0214 — Visitor

Use a visitor to sum the values of a small node tree with leaves 1, 2, 3, printing `6`. A `Visitor` trait visits each node via an `accept` method (double dispatch).

## Run

    cargo run
