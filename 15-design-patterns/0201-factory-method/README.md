# 0201 — Factory Method

Use a factory method to create shapes by name and print their kinds `circle square`. In Rust a function returns `Box<dyn Shape>` trait objects keyed by name.

## Run

    cargo run
