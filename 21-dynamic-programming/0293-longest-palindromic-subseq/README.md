# 0293 — Longest Palindromic Subsequence

Compute the longest palindromic subsequence length of "bbbab", printing `4`. Rust collects the string into a `Vec<u8>` and fills the interval DP bottom-up.

## Run

    cargo run
