# 0296 — Matrix Chain Order

Find the minimum scalar multiplications to multiply matrices with dimensions [10,20,30,40], printing `18000`. Rust fills the interval DP by increasing chain length and folds the split minimum with `min`.

## Run

    cargo run
