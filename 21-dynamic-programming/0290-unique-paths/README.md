# 0290 — Unique Grid Paths

Count lattice paths from the top-left to bottom-right of a 3x3 grid moving right/down, printing `6`. Rust keeps a single rolling `Vec<u64>` row and adds in place.

## Run

    cargo run
