# 0291 — Minimum Path Sum

Find the minimum path sum through [[1,3,1],[1,5,1],[4,2,1]] moving right/down, printing `7`. Rust handles the grid borders with a `match` on the `(row, col)` tuple.

## Run

    cargo run
