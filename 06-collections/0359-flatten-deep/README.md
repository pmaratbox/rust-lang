# 0359 — Flatten Deeply

Flatten the arbitrarily nested structure [1,[2,[3,4]],5] into `1 2 3 4 5`. Rust models the nesting with a recursive `enum` and pattern-matches each variant.

## Run

    cargo run
