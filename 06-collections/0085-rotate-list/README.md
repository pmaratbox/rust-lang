# 0085 — Rotate a List

Rotate the list `1, 2, 3, 4, 5` left by `2` positions (elements wrap to the end) and print it: `3 4 5 1 2`. `Vec::rotate_left(2)` rotates the elements left in place — a built-in slice operation.

## Run

    cargo run
