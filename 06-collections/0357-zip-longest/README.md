# 0357 — Zip Longest

Zip [1,2,3] with ["a","b"], filling the missing slot with "-", printing `1a 2b 3-`. Rust uses `slice::get` returning `Option` to detect the exhausted side.

## Run

    cargo run
