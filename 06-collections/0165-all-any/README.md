# 0165 — All and Any

For [2,4,6] report whether all are even (yes) and whether any are odd (no), printing `yes no`. Rust's iterator `all` and `any` short-circuit over the predicate.

## Run

    cargo run
