# 0083 — Sliding Window

Slide a window of size `2` over `1, 2, 3, 4` and print each window on its own line: `1 2`, `2 3`, `3 4`. `slice::windows(2)` yields every overlapping length-2 subslice — the built-in sliding window.

## Run

    cargo run
