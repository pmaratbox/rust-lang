# 0170 — Index By Key

Index people [(1,alice),(2,bob)] by id into a map, look up id 2, and print `id 2: bob`. Collecting `(key, value)` pairs into a `HashMap` builds the index directly.

## Run

    cargo run
