# 0668 — Factorial

Compute `30!` exactly by multiplying `1..=30` into a `num_bigint::BigInt`,
an arbitrary-precision integer that grows past the range of any fixed-width
type. The product is exact (no overflow, no rounding) and prints all 33 digits.

## Run

    cargo run
