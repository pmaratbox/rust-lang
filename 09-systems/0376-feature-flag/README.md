# 0376 — Feature Flag

Branch on a boolean feature flag: on -> "enabled", off -> "disabled", printing `enabled disabled`. Rust uses an `if` expression that yields the label for each flag value.

## Run

    cargo run
