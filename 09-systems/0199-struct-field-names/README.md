# 0199 — Struct Field Names

List the field names of a Point{x,y} struct/record and print `x y`. Rust has no runtime reflection, so the field names are an associated `FIELDS` constant declared alongside the struct in field order.

## Run

    cargo run
