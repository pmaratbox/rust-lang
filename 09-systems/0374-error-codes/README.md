# 0374 — Error Codes

Map the numeric error code 2 to its message "not found" and print `not found`. Rust expresses the code-to-message lookup as a `match` on the integer code.

## Run

    cargo run
