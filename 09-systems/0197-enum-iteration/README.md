# 0197 — Enum Iteration

Iterate over all values of a Color enum (RED, GREEN, BLUE) and print their names `RED GREEN BLUE`. Rust has no built-in enum iteration, so an explicit `ALL` array of variants is the idiomatic way to walk them in declaration order.

## Run

    cargo run
