# 0134 — FizzBuzz

Print 1..15, replacing multiples of 3 with Fizz, of 5 with Buzz, and of both with FizzBuzz, one per line. Idiomatic Rust matches on a `(n % 3, n % 5)` tuple.

## Run

    cargo run
