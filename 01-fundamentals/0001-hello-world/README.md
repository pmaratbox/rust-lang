# 0001 — Hello, world!

The minimal Rust program: a `main` function using the `println!` macro.
`Cargo.toml` defines the package and pins a binary named `hello` so the
build artifact has a stable name.

## Run

    cargo run
