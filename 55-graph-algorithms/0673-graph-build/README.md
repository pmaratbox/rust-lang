# 0673 — Build a graph

Build the fixed weighted undirected graph `G` with `petgraph`, Rust's graph data
structure and algorithm library. Using `UnGraphMap::from_edges`, we construct the
graph from its edge list, then ask the library for `node_count()` and
`edge_count()` and print them space-joined.

## Run

    cargo run
