# 0315 — Tree Diameter

Compute the diameter (longest path in edges) of a tree where root has children A and B, and A has children C and D, printing `3`. A single recursive pass returns the height while a `&mut` accumulator tracks the best diameter.

## Run

    cargo run
