# 0318 — Kth Largest

Find the 2nd largest element of [3,2,1,5,6,4] using a heap, printing `5`. A `BinaryHeap` of `Reverse<i64>` acts as a bounded min-heap of the largest seen.

## Run

    cargo run
