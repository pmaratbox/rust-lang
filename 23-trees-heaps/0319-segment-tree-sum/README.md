# 0319 — Segment Tree Range Sum

Build a segment tree over [1,2,3,4,5] and query the sum of indices 1..3 (inclusive), printing `9`. A flat `Vec` stores the tree with `2*i`/`2*i+1` child indexing.

## Run

    cargo run
