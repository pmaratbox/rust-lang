# 0309 — Validate BST

Check whether two trees are valid BSTs: a correct one (yes) and one with an out-of-place node (no), printing `yes no`. Rust enums model the optional child links cleanly with `Box<Node>`.

## Run

    cargo run
