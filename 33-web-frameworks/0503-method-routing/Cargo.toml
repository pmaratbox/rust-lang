[package]
name = "method_routing"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "method_routing"
path = "src/main.rs"

[dependencies]
axum = "0.7"
tokio = { version = "1", features = ["full"] }
tower = { version = "0.5", features = ["util"] }
