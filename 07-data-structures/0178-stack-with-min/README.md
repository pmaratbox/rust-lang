# 0178 — Stack With Min

Build a stack that tracks its minimum in O(1); after pushing 3,1,2 print `min: 1`. An auxiliary `mins` vector mirrors the data stack, each entry caching the running minimum.

## Run

    cargo run
