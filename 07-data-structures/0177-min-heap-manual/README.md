# 0177 — Min Heap (Manual)

Implement a binary min-heap by hand, push 3,1,2, pop all, printing `1 2 3`. A `Vec<i32>` backs the complete tree, with sift-up on push and sift-down on pop restoring the heap property.

## Run

    cargo run
