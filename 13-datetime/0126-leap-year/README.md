# 0126 — Leap Year Check

Determine whether 2000, 1900, and 2024 are leap years, printing `yes no yes`. Rust expresses the rule as a single boolean expression returned from a small helper `fn`.

## Run

    cargo run
