# 0393 — Weekday After N Days

Given 2000-01-01 is a Saturday, compute the weekday 3 days later, printing `Tuesday`. Rust indexes a fixed-size array of names with a modular arithmetic result.

## Run

    cargo run
