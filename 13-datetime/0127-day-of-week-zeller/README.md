# 0127 — Day of Week (Zeller)

Use Zeller’s congruence to compute the weekday of 2000-01-01 and print `Saturday`. The result indexes a fixed array of `&'static str` weekday names, avoiding any allocation.

## Run

    cargo run
