# 0130 — Format ISO Date

Format the date y=2026, m=6, d=4 as a zero-padded ISO string `2026-06-04`. Rust's `{:02}` format specifier handles the zero-padding directly inside `println!`.

## Run

    cargo run
