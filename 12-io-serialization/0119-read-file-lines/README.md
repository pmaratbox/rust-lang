# 0119 — Read File Lines

Write three lines to a file, read them back, and print `lines: 3`. `fs::read_to_string` plus the `lines()` iterator makes counting non-empty lines trivial.

## Run

    cargo run
