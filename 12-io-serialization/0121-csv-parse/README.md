# 0121 — Parse CSV

Parse the two CSV rows `alice,30` and `bob,25` into name=value pairs and print `alice=30 bob=25`. `str::split_once` cleanly separates each row on its first comma.

## Run

    cargo run
