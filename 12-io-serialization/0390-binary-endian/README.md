# 0390 — Big-Endian Bytes

Encode the integer 258 as two big-endian bytes (1, 2), decode them back to 258, printing `1 2 258`. Rust uses the built-in `to_be_bytes` and `from_be_bytes` methods on `u16`.

## Run

    cargo run
