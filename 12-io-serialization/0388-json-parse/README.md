# 0388 — JSON Parse

Parse the JSON object `{"x":1,"y":2}` and print its entries `x=1 y=2`. Rust splits the flat object on commas and colons using iterator adapters, preserving order.

## Run

    cargo run
