# 0079 — Character Codes

Print the character code of `A` and the character for code `66`: `65 B`. `'A' as u32` gives the scalar value; `66 as u8 as char` converts a byte to its character (only valid for ASCII/Latin-1 ranges).

## Run

    cargo run
