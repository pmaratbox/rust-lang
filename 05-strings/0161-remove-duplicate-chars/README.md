# 0161 — Remove Duplicate Characters

Remove duplicate characters from "aabbcc" keeping first occurrence, printing `abc`. A `HashSet::insert` returns whether the value was new, so it doubles as a filter that preserves first-seen order.

## Run

    cargo run
