# 0159 — String Interpolation

Interpolate name="Ada" and age=36 into a sentence, printing `Ada is 36`. Rust's `println!` supports inline `{name}` captured-identifier interpolation.

## Run

    cargo run
