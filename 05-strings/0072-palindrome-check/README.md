# 0072 — Palindrome Check

Check whether each of `level` and `hello` reads the same forwards and backwards, printing `level: yes` and `hello: no`. Collecting the `chars()` forwards and reversed into two `Vec<char>`s and comparing them tests the palindrome over Unicode scalars.

## Run

    cargo run
