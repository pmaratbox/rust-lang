# 0352 — ROT13

Apply ROT13 to "hello" (->"uryyb") and ROT13 again to recover "hello", printing `uryyb hello`. Rust pattern-matches char ranges and maps bytes via modular arithmetic.

## Run

    cargo run
