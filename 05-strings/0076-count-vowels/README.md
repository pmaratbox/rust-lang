# 0076 — Count Vowels

Count the vowels (`a`, `e`, `i`, `o`, `u`) in the word `hello` and print the total: `2`. `filter` keeps the characters for which `"aeiou".contains(*c)` is true, and `count` tallies them.

## Run

    cargo run
