# 0156 — Count Substring Occurrences

Count non-overlapping occurrences of "ab" in "ababab", printing `3`. The `str::matches` iterator yields non-overlapping matches, so counting them is idiomatic.

## Run

    cargo run
