# 0077 — Title Case

Convert `hello world` to title case (uppercase the first letter of each word) and print it: `Hello World`. `chars.next()` takes the first character to uppercase, then `chars.as_str()` appends the untouched remainder of the word.

## Run

    cargo run
