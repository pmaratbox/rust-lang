# 0078 — Substring Search

Find the index of the first occurrence of `lo` in `hello` and print it: `3`. `str::find` returns `Option<usize>` (a byte index); `unwrap` extracts the position, which exists here.

## Run

    cargo run
