# 0353 — Character Frequency

Count character frequencies in "aab" and print them in first-seen order `a:2 b:1`. Rust tracks first-appearance order in a Vec alongside a HashMap of counts.

## Run

    cargo run
