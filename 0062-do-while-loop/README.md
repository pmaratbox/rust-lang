# 0062 — Do-While Loop

Use a do-while loop (whose body runs once before the condition is checked) to print `1`, `2`, and `3` on one line: `1 2 3`. Rust has no do-while; `loop { ... }` with a `break` runs the body unconditionally, testing at the bottom.

## Run

    cargo run
