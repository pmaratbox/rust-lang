# 0100 — Static Methods

Define a static (class-level) method `square` that needs no instance, call it as `square(6)`, and print `36`. An associated function (one with no `self` parameter) is called with `Type::function(...)` — Rust's equivalent of a static method.

## Run

    cargo run
