# 0348 — Interface Default Method

Define an interface with a default greet() returning "hi" and an implementer that overrides it to "hey", printing `hi hey`. In Rust a trait method body provides the default and an `impl` block overrides it.

## Run

    cargo run
