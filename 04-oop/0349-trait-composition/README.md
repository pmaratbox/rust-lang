# 0349 — Trait Composition

Compose two capabilities (A printing "a", B printing "b") into one type and invoke both, printing `a b`. In Rust one struct can implement multiple traits to gain each capability.

## Run

    cargo run
