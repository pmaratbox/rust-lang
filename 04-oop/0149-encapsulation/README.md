# 0149 — Encapsulation

Keep an account balance private, deposit 50 onto an initial 100 through a method, and print `150`. In Rust, struct fields are private by default within their module, so access flows through methods.

## Run

    cargo run
