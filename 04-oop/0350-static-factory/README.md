# 0350 — Static Factory Method

Use a static factory Color.fromHex("#ff0000") to build a color and print its components `255 0 0`. In Rust an associated function `Color::from_hex` parses the hex pairs with `u8::from_str_radix`.

## Run

    cargo run
