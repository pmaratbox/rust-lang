# 0189 — Permutations

Generate all permutations of [1,2,3] in lexicographic order, one per line space-separated. Recursion picks remaining elements in order, preserving lexicographic output.

## Run

    cargo run
