# 0094 — Linear Search

Linearly scan the array `4, 2, 7, 1, 9` for `7` and print the index where it is found: `found 7 at index 2`. `Iterator::position` scans for the first element matching the predicate, returning its index as an `Option`.

## Run

    cargo run
