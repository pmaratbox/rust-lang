# 0190 — Subsets (Power Set)

Generate the power set of [1,2,3] in bitmask order 0..7 (empty printed as `{}`), one subset per line space-separated. A `1 << i` bit test selects each element per mask.

## Run

    cargo run
