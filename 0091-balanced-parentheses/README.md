# 0091 — Balanced Parentheses

Using a stack, check whether `(())` is balanced and whether `(()` is balanced, printing `yes` then `no`. `Vec::pop` returns `Option`; popping `None` on a `)` means an unmatched close, and a non-empty stack at the end means unbalanced.

## Run

    cargo run
