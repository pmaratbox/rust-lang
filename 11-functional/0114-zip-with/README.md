# 0114 — Zip With

Combine [1,2,3] and [4,5,6] element-wise with addition, printing `5 7 9`. Iterator `zip` pairs the slices and `map` adds each pair before joining with spaces.

## Run

    cargo run
