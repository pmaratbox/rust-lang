# 0386 — Iterate and Take

Iterate f(x)=x*3 from 1 and take the first four values, printing `1 3 9 27`. Idiomatic Rust uses `std::iter::successors` to iterate a function and `take` to limit it.

## Run

    cargo run
