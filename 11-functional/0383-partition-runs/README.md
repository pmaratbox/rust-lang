# 0383 — Partition Into Runs

Partition [1,1,2,3,3,3] into runs of equal adjacent elements, printing `1 1|2|3 3 3`. Idiomatic Rust uses `last_mut` on a `Vec<Vec<i32>>` to extend the current run.

## Run

    cargo run
