# 0061 — Reverse a String

Reverse the string `abc` character by character and print the result: `cba`. `chars()` iterates Unicode scalar values and `rev().collect()` reverses them into a new `String`; grapheme clusters (e.g. combined emoji) would need an external crate.

## Run

    cargo run
